# Hypemove Landing (Vite + React + Tailwind)

## Setup
```bash
npm i
npm run dev
```

## Build
```bash
npm run build && npm run preview
```

Sostituisci i placeholder delle immagini con screenshot reali dell'app.
